'use client'

/**
 * Demo Banner
 *
 * Previously showed a fixed banner at the top indicating demo mode.
 * Now removed to keep demo experience identical to production.
 * Demo mode is indicated by "Familjen Demo" text in the header instead.
 */

export function DemoBanner() {
  // Demo indicator moved to header ("Familjen Demo" text)
  // Keeping component for potential future use or easy re-enablement
  return null
}
